@div class=flex items-center gap-12 w-full max-w-6xl mx-auto px-6
  @div class=flex-1
    @h1 class=text-5xl font-bold leading-tight
      {title}
    @p class=py-6 text-lg opacity-80
      {description}
    @div class=flex gap-4
      {cta}

  @div class=flex-1
    {image}

