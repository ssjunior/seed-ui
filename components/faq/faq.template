@div class=max-w-5xl mx-auto flex flex-col gap-1 px-4 sm:px-6 text-center
  @h2 as=h2, class=text-2xl sm:text-3xl md:text-4xl font-bold text-gray-900
      {?title}

  @p as=p, class=text-gray-600 text-base sm:text-lg
      {?description}

  @div class=flex flex-col
    {?children}
