@aside class=flex flex-col h-screen w-64
  @div class=flex items-center h-16 px-4 border-b border-slate-200
    {?logo}
  @nav as=nav, class=flex-1 overflow-y-auto py-4 px-2 space-y-1
    {?children}
  @div class=p-4 border-t border-slate-200
    {?footer}
