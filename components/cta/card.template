@div class=max-w-6xl mx-auto px-4 sm:px-6 lg:px-8
  @div class=bg-gradient-to-r from-teal-600 to-teal-700 rounded-2xl sm:rounded-3xl p-8 sm:p-12 md:p-16 text-white text-center
    @h2 as=h2, class=text-2xl sm:text-3xl md:text-4xl font-bold
      {?title}
    @p as=p, class=text-base sm:text-lg opacity-90 mt-4 mb-6 sm:mb-8 max-w-xl mx-auto
      {?description}
    @div class=flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center
      {?actions}
