@div class=max-w-3xl mx-auto px-4 sm:px-6 text-center
  @h2 as=h2, class=text-2xl sm:text-3xl md:text-4xl font-bold
    {?title}
  @p as=p, class=text-base sm:text-lg opacity-90 mt-4 mb-6 sm:mb-8
    {?description}
  @div class=flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center
    {?actions}
