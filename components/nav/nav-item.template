@a href={?href}, class=seed-nav-item flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-medium text-gray-700 hover:bg-gray-100 hover:text-gray-900 transition-colors cursor-pointer data-[active]:bg-teal-50 data-[active]:text-teal-700
  {?icon}
  @span class=text-base font-semibold
    {?title}
@div class=absolute left-0 top-full pt-1 hidden z-50 group-hover:block
  @div class=bg-white rounded-xl shadow-lg border border-gray-200 p-2
    @div class=grid grid-cols-3 gap-1 w-[560px]
      {?children}
